scene/
# Audio preprocessing — only the final fpv_loop.wav is committed. The raw
# source clip, the prep script, and any tooling artefacts stay local.
asset/music/fpv.wav
# Source archives for BGM tracks — the decoded .flac files are tracked but
# the zips / originals are not (too large).
asset/music/*.zip
tools/
